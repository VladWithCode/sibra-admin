import React from 'react';
import Section from '../Section/Section';

function StatSection({ project }) {
    return (
        <Section
            heading="Estadisticas"
            bodyClass="fs-4 fw-500"
            useCard={true}
            cardShadow={true}
        >
            uwu
        </Section>
    );
}

export default StatSection;
